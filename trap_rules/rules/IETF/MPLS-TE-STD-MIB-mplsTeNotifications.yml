- mapping: |-
    #!blobl
    root = this
    root.out.origin.agent.name = "MPLS-TE-STD-MIB"
- switch:
    - check: this.trap.SpecificTrap == 1
      processors:
        - label: mpls_tunnel_up
          mapping: |-
            #!blobl
            root = this


            root.out.IETF.mplsTunnelAdminStatus = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.34")
            root.out.IETF.mplsTunnelOperStatus = this.trap.VarBinds.index(1).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.35")
        - label: mpls_tunnel_up_nested_switch
          switch:
            - check: ' == .1.3.6.1.2.1.10.166.3'
              processors:
                - label: mpls_tunnel_up_nested_switch_.1.3.6.1.2.1.10.166.3
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == .1.3.6.1.3.95.3'
              processors:
                - label: mpls_tunnel_up_nested_switch_.1.3.6.1.3.95.3
                  mapping: |+
                    #!blobl
                    root = this

            - processors:
                - label: mpls_tunnel_up_nested_switch_default
                  mapping: |+
                    #!blobl
                    root = this

        - label: mpls_tunnel_up_after_switch
          mapping: |
            #!blobl
            root = this

            root.out.event.class.name = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelUp"
            root.out.event.id = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelUp"
            root.out.event.category.name = "MPLS Tunnel Status"
            root.out.object.name = "mplsTunnelEntry.$mplsTunnelIndex.$mplsTunnelInstance.$mplsTunnelIngressLSRId.$mplsTunnelEgressLSRId
            root.out.event.message = "MPLS Tunnel Up  ( Ingress LSR: $mplsTunnelIngressLSRId, Egress LSR: $mplsTunnelEgressLSRId, Tunnel Index: $mplsTunnelIndex, Tunnel Instance: $mplsTunnelInstance )"
            root.out.event.severity.code = 1
            root.out.event.severity.level = "Alert"
    - check: this.trap.SpecificTrap == 2
      processors:
        - label: mpls_tunnel_down
          mapping: |-
            #!blobl
            root = this


            root.out.IETF.mplsTunnelAdminStatus = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.34")
            root.out.IETF.mplsTunnelOperStatus = this.trap.VarBinds.index(1).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.35")
        - label: mpls_tunnel_down_nested_switch
          switch:
            - check: ' == .1.3.6.1.2.1.10.166.3'
              processors:
                - label: mpls_tunnel_down_nested_switch_.1.3.6.1.2.1.10.166.3
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == .1.3.6.1.3.95.3'
              processors:
                - label: mpls_tunnel_down_nested_switch_.1.3.6.1.3.95.3
                  mapping: |+
                    #!blobl
                    root = this

            - processors:
                - label: mpls_tunnel_down_nested_switch_default
                  mapping: |+
                    #!blobl
                    root = this

        - label: mpls_tunnel_down_after_switch
          mapping: |
            #!blobl
            root = this

            root.out.event.class.name = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelDown"
            root.out.event.id = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelDown"
            root.out.event.category.name = "MPLS Tunnel Status"
            root.out.object.name = "mplsTunnelEntry.$mplsTunnelIndex.$mplsTunnelInstance.$mplsTunnelIngressLSRId.$mplsTunnelEgressLSRId
            root.out.event.message = "MPLS Tunnel Down  ( Ingress LSR: $mplsTunnelIngressLSRId, Egress LSR: $mplsTunnelEgressLSRId, Tunnel Index: $mplsTunnelIndex, Tunnel Instance: $mplsTunnelInstance )"
            root.out.event.severity.code = 4
            root.out.event.severity.level = "Warning"
    - check: this.trap.SpecificTrap == 3
      processors:
        - label: mpls_tunnel_rerouted
          mapping: |-
            #!blobl
            root = this


            root.out.IETF.mplsTunnelAdminStatus = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.34")
            root.out.IETF.mplsTunnelOperStatus = this.trap.VarBinds.index(1).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.35")
        - label: mpls_tunnel_rerouted_nested_switch
          switch:
            - check: ' == .1.3.6.1.2.1.10.166.3'
              processors:
                - label: mpls_tunnel_rerouted_nested_switch_.1.3.6.1.2.1.10.166.3
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == .1.3.6.1.3.95.3'
              processors:
                - label: mpls_tunnel_rerouted_nested_switch_.1.3.6.1.3.95.3
                  mapping: |+
                    #!blobl
                    root = this

            - processors:
                - label: mpls_tunnel_rerouted_nested_switch_default
                  mapping: |+
                    #!blobl
                    root = this

        - label: mpls_tunnel_rerouted_after_switch
          mapping: |
            #!blobl
            root = this

            root.out.event.severity.code = 1
            root.out.event.severity.level = "Alert"
            root.out.event.class.name = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelRerouted"
            root.out.event.id = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelRerouted"
            root.out.event.category.name = "MPLS Tunnel Status"
            root.out.object.name = "mplsTunnelEntry.$mplsTunnelIndex.$mplsTunnelInstance.$mplsTunnelIngressLSRId.$mplsTunnelEgressLSRId
            root.out.event.message = "MPLS Tunnel Re-Routed  ( Ingress LSR: $mplsTunnelIngressLSRId, Egress LSR: $mplsTunnelEgressLSRId, Tunnel Index: $mplsTunnelIndex, Tunnel Instance: $mplsTunnelInstance )"
    - check: this.trap.SpecificTrap == 4
      processors:
        - label: mpls_tunnel_reoptimized
          mapping: |-
            #!blobl
            root = this


            root.out.IETF.mplsTunnelAdminStatus = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.34")
            root.out.IETF.mplsTunnelOperStatus = this.trap.VarBinds.index(1).Value.enum_enrich(".1.3.6.1.2.1.10.166.3.2.2.1.35")
        - label: mpls_tunnel_reoptimized_nested_switch
          switch:
            - check: ' == .1.3.6.1.2.1.10.166.3'
              processors:
                - label: mpls_tunnel_reoptimized_nested_switch_.1.3.6.1.2.1.10.166.3
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == .1.3.6.1.3.95.3'
              processors:
                - label: mpls_tunnel_reoptimized_nested_switch_.1.3.6.1.3.95.3
                  mapping: |+
                    #!blobl
                    root = this

            - processors:
                - label: mpls_tunnel_reoptimized_nested_switch_default
                  mapping: |+
                    #!blobl
                    root = this

        - label: mpls_tunnel_reoptimized_after_switch
          mapping: |
            #!blobl
            root = this

            root.out.event.class.name = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelReoptimized"
            root.out.event.id = "SNMPTRAP-IETF-MPLS-TE-STD-MIB-mplsTunnelReoptimized"
            root.out.event.category.name = "MPLS Tunnel Status"
            root.out.object.name = "mplsTunnelEntry.$mplsTunnelIndex.$mplsTunnelInstance.$mplsTunnelIngressLSRId.$mplsTunnelEgressLSRId
            root.out.event.message = "MPLS Tunnel Re-Optimized  ( Ingress LSR: $mplsTunnelIngressLSRId, Egress LSR: $mplsTunnelEgressLSRId, Tunnel Index: $mplsTunnelIndex, Tunnel Instance: $mplsTunnelInstance )"
            root.out.event.severity.code = 2
            root.out.event.severity.level = "Critical"
    - processors:
        - label: default
          mapping: |
            #!blobl,
            root = this
            root.out.event.category.name = "Unknown Trap"
