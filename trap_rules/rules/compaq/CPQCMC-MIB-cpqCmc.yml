- mapping: |-
    #!blobl
    root = this
    root.out.origin.agent.name = "CPQCMC-MIB"
- switch:
    - check: this.trap.SpecificTrap == 153001
      processors:
        - label: cpq_cmcalarm_temp1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Temperature Status"
            root.out.object.name = "Temperature 1"

            root.out.compaq.cpqCmcStatusTemp1 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.1")
        - label: cpq_cmcalarm_temp1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Status Unknown  ( Temperature 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Normal  ( Temperature 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Warning  ( Temperature 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Over Maximum  ( Temperature 1 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Under Minimum  ( Temperature 1 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Temperature Sensor Connected  ( Temperature 1 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_temp1_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Sensor Error  ( Temperature 1 )"
            - processors:
                - label: cpq_cmcalarm_temp1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Status Unknown  ( Temperature 1 )"
        - label: cpq_cmcalarm_temp1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153002
      processors:
        - label: cpq_cmcalarm_temp2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Temperature Status"
            root.out.object.name = "Temperature 2"

            root.out.compaq.cpqCmcStatusTemp2 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.2")
        - label: cpq_cmcalarm_temp2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Status Unknown  ( Temperature 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Normal  ( Temperature 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Warning  ( Temperature 2 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Over Maximum  ( Temperature 2 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Under Minimum  ( Temperature 2 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Temperature Sensor Connected  ( Temperature 2 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_temp2_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Sensor Error  ( Temperature 2 )"
            - processors:
                - label: cpq_cmcalarm_temp2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Temperature Status Unknown  ( Temperature 2 )"
        - label: cpq_cmcalarm_temp2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153003
      processors:
        - label: cpq_cmcalarm_fan1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Fan Status"
            root.out.object.name = "Fan 1"

            root.out.compaq.cpqCmcStatusFan1 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.3")
        - label: cpq_cmcalarm_fan1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Status Unknown  ( Fan 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off by Temperature Sensor 1  ( Fan 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched On by Temperature Sensor 1  ( Fan 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off Manually  ( Fan 1 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched On Manually  ( Fan 1 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off, Smoke Alarm  ( Fan 1 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off, Door Open  ( Fan 1 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Not Present  ( Fan 1 )"
            - check: ' == 9'
              processors:
                - label: cpq_cmcalarm_fan1_nested_switch_9
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Error  ( Fan 1 )"
            - processors:
                - label: cpq_cmcalarm_fan1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Status Unknown  ( Fan 1 )"
        - label: cpq_cmcalarm_fan1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153004
      processors:
        - label: cpq_cmcalarm_fan2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Fan Status"
            root.out.object.name = "Fan 2"

            root.out.compaq.cpqCmcStatusFan2 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.4")
        - label: cpq_cmcalarm_fan2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Status Unknown  ( Fan 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off by Temperature Sensor 2  ( Fan 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched On by Temperature Sensor 2  ( Fan 2 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off Manually  ( Fan 2 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched On Manually  ( Fan 2 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off, Smoke Alarm  ( Fan 2 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Switched Off, Door Open  ( Fan 2 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Not Present  ( Fan 2 )"
            - check: ' == 9'
              processors:
                - label: cpq_cmcalarm_fan2_nested_switch_9
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Error  ( Fan 2 )"
            - processors:
                - label: cpq_cmcalarm_fan2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Fan Status Unknown  ( Fan 2 )"
        - label: cpq_cmcalarm_fan2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153005
      processors:
        - label: cpq_cmcalarm_voltage
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Voltage Status"
            root.out.object.name = ""

            root.out.compaq.cpqCmcStatusVoltage = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.5")
        - label: cpq_cmcalarm_voltage_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_voltage_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Voltage Status Unknown"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_voltage_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Voltage Normal"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_voltage_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Voltage Over Maximum"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_voltage_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Voltage Under Minimum"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_voltage_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Power Connected"
            - processors:
                - label: cpq_cmcalarm_voltage_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Voltage Status Unknown"
        - label: cpq_cmcalarm_voltage_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153006
      processors:
        - label: cpq_cmcalarm_humidity
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Humidity Status"
            root.out.object.name = ""

            root.out.compaq.cpqCmcStatusHumidity = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.6")
        - label: cpq_cmcalarm_humidity_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Status Unknown"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Normal"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Over Maximum"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Under Minimum"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Humidity Sensor Connected"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_humidity_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Sensor Error"
            - processors:
                - label: cpq_cmcalarm_humidity_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Humidity Status Unknown"
        - label: cpq_cmcalarm_humidity_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153007
      processors:
        - label: cpq_cmcalarm_input1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door/Sidepanel Status"
            root.out.object.name = "Door/Sidepanel 1"

            root.out.compaq.cpqCmcStatusInput1 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.7")
        - label: cpq_cmcalarm_input1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_input1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_input1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Closed  ( Door/Sidepanel 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_input1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Open  ( Door/Sidepanel 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_input1_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Door/Sidepanel Sensor Connected  ( Door/Sidepanel 1 )"
            - processors:
                - label: cpq_cmcalarm_input1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 1 )"
        - label: cpq_cmcalarm_input1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153008
      processors:
        - label: cpq_cmcalarm_input2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door/Sidepanel Status"
            root.out.object.name = "Door/Sidepanel 2"

            root.out.compaq.cpqCmcStatusInput2 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.8")
        - label: cpq_cmcalarm_input2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_input2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_input2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Closed  ( Door/Sidepanel 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_input2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Open  ( Door/Sidepanel 2 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_input2_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Door/Sidepanel Sensor Connected  ( Door/Sidepanel 2 )"
            - processors:
                - label: cpq_cmcalarm_input2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 2 )"
        - label: cpq_cmcalarm_input2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153009
      processors:
        - label: cpq_cmcalarm_input3
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door/Sidepanel Status"
            root.out.object.name = "Door/Sidepanel 3"

            root.out.compaq.cpqCmcStatusInput3 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.9")
        - label: cpq_cmcalarm_input3_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_input3_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 3 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_input3_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Closed  ( Door/Sidepanel 3 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_input3_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Open  ( Door/Sidepanel 3 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_input3_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Door/Sidepanel Sensor Connected  ( Door/Sidepanel 3 )"
            - processors:
                - label: cpq_cmcalarm_input3_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 3 )"
        - label: cpq_cmcalarm_input3_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153010
      processors:
        - label: cpq_cmcalarm_input4
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door/Sidepanel Status"
            root.out.object.name = "Door/Sidepanel 4"

            root.out.compaq.cpqCmcStatusInput4 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.10")
        - label: cpq_cmcalarm_input4_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_input4_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 4 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_input4_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Closed  ( Door/Sidepanel 4 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_input4_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Open  ( Door/Sidepanel 4 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_input4_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Door/Sidepanel Sensor Connected  ( Door/Sidepanel 4 )"
            - processors:
                - label: cpq_cmcalarm_input4_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door/Sidepanel Unknown  ( Door/Sidepanel 4 )"
        - label: cpq_cmcalarm_input4_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153011
      processors:
        - label: cpq_cmcalarm_lock1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door Lock Status"
            root.out.object.name = "Door Lock 1"

            root.out.compaq.cpqCmcStatusLock1Lock = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.11")
        - label: cpq_cmcalarm_lock1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Status Unknown  ( Door Lock 1 )"
            - check: ' == 10'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_10
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Connection Failure  ( Door Lock 1 )"
            - check: ' == 11'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_11
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Activated but Not Locked  ( Door Lock 1 )"
            - check: ' == 12'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_12
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locking Device Configuration Error  ( Door Lock 1 )"
            - check: ' == 13'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_13
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Not Installed  ( Door Lock 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locked  ( Door Lock 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Network Management  ( Door Lock 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Network Management  ( Door Lock 1 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Smoke Alarm  ( Door Lock 1 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Key  ( Door Lock 1 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Power Failure of Locking Device  ( Door Lock 1 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Low Battery of Locking Device  ( Door Lock 1 )"
            - check: ' == 9'
              processors:
                - label: cpq_cmcalarm_lock1_nested_switch_9
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Network Failure  ( Door Lock 1 )"
            - processors:
                - label: cpq_cmcalarm_lock1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Status Unknown  ( Door Lock 1 )"
        - label: cpq_cmcalarm_lock1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153012
      processors:
        - label: cpq_cmcalarm_lock2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door Lock Status"
            root.out.object.name = "Door Lock 2"

            root.out.compaq.cpqCmcStatusLock2Lock = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.12")
        - label: cpq_cmcalarm_lock2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Status Unknown  ( Door Lock 2 )"
            - check: ' == 10'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_10
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Connection Failure  ( Door Lock 2 )"
            - check: ' == 11'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_11
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Activated but Not Locked  ( Door Lock 2 )"
            - check: ' == 12'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_12
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locking Device Configuration Error  ( Door Lock 2 )"
            - check: ' == 13'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_13
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Not Installed  ( Door Lock 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locked  ( Door Lock 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Network Management  ( Door Lock 2 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Network Management  ( Door Lock 2 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Smoke Alarm  ( Door Lock 2 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked by Key  ( Door Lock 2 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Power Failure of Locking Device  ( Door Lock 2 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Low Battery of Locking Device  ( Door Lock 2 )"
            - check: ' == 9'
              processors:
                - label: cpq_cmcalarm_lock2_nested_switch_9
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Network Failure  ( Door Lock 2 )"
            - processors:
                - label: cpq_cmcalarm_lock2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Status Unknown  ( Door Lock 2 )"
        - label: cpq_cmcalarm_lock2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153013
      processors:
        - label: cpq_cmcalarm_smoke
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Smoke Detector Status"
            root.out.object.name = ""

            root.out.compaq.cpqCmcStatusSmoke = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.13")
        - label: cpq_cmcalarm_smoke_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_smoke_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Smoke Detector Status Unknown"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_smoke_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Smoke Detector Cleared"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_smoke_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Smoke Detected"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_smoke_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Smoke Detector Connected"
            - processors:
                - label: cpq_cmcalarm_smoke_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Smoke Detector Status Unknown"
        - label: cpq_cmcalarm_smoke_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153014
      processors:
        - label: cpq_cmcalarm_shock
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Shock Sensor Status"
            root.out.object.name = ""

            root.out.compaq.cpqCmcStatusShock = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.14")
        - label: cpq_cmcalarm_shock_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_shock_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Shock Sensor Status Unknown"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_shock_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Shock Sensor Cleared"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_shock_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Shock Detected"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_shock_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No Shock Sensor Connected"
            - processors:
                - label: cpq_cmcalarm_shock_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Shock Sensor Status Unknown"
        - label: cpq_cmcalarm_shock_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153015
      processors:
        - label: cpq_cmcalarm_aux1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "CMC Auxilliary Alarm"
            root.out.object.name = "Auxilliary Alarm 1"

            root.out.compaq.cpqCmcStatusAux1 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.15")
        - label: cpq_cmcalarm_aux1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_aux1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Status Unknown  ( Auxilliary Alarm 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_aux1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Cleared  ( Auxilliary Alarm 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_aux1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Detected  ( Auxilliary Alarm 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_aux1_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No CMC Auxilliary Alarm Sensor Connected  ( Auxilliary Alarm 1 )"
            - processors:
                - label: cpq_cmcalarm_aux1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Status Unknown  ( Auxilliary Alarm 1 )"
        - label: cpq_cmcalarm_aux1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153016
      processors:
        - label: cpq_cmcalarm_aux2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "CMC Auxilliary Alarm"
            root.out.object.name = "Auxilliary Alarm 2"

            root.out.compaq.cpqCmcStatusAux2 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.16")
        - label: cpq_cmcalarm_aux2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_aux2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Status Unknown  ( Auxilliary Alarm 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_aux2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Cleared  ( Auxilliary Alarm 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_aux2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Detected  ( Auxilliary Alarm 2 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_aux2_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "No CMC Auxilliary Alarm Sensor Connected  ( Auxilliary Alarm 2 )"
            - processors:
                - label: cpq_cmcalarm_aux2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Auxilliary Alarm Status Unknown  ( Auxilliary Alarm 2 )"
        - label: cpq_cmcalarm_aux2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153017
      processors:
        - label: cpq_cmcalarm1
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "CMC Alarm Status"
            root.out.object.name = "Alarm 1"

            root.out.compaq.cpqCmcStatusAlarm1 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.17")
        - label: cpq_cmcalarm1_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm1_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Status Unknown  ( Alarm 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm1_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Cleared  ( Alarm 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm1_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Detected  ( Alarm 1 )"
            - processors:
                - label: cpq_cmcalarm1_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Status Unknown  ( Alarm 1 )"
        - label: cpq_cmcalarm1_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153018
      processors:
        - label: cpq_cmcalarm2
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "CMC Alarm Status"
            root.out.object.name = "Alarm 2"

            root.out.compaq.cpqCmcStatusAlarm2 = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.18")
        - label: cpq_cmcalarm2_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm2_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Status Unknown  ( Alarm 2 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm2_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Cleared  ( Alarm 2 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm2_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Detected  ( Alarm 2 )"
            - processors:
                - label: cpq_cmcalarm2_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "CMC Alarm Status Unknown  ( Alarm 2 )"
        - label: cpq_cmcalarm2_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153019
      processors:
        - label: cpq_cmcalarm_lock1dev
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door Lock Status"
            root.out.object.name = "Door Lock 1"

            root.out.compaq.cpqCmcStatusLock1Dev = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.19")
        - label: cpq_cmcalarm_lock1dev_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Status Unknown  ( Door Lock 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locked  ( Door Lock 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Power Failure of Locking Device  ( Door Lock 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Low Battery of Locking Device  ( Door Lock 1 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Locking Device Battery Needs to be Replaced  ( Door Lock 1 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Locking Device Battery Missing  ( Door Lock 1 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Connection Lost to Locking Device  ( Door Lock 1 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Not Installed  ( Door Lock 1 )"
            - processors:
                - label: cpq_cmcalarm_lock1dev_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Status Unknown  ( Door Lock 1 )"
        - label: cpq_cmcalarm_lock1dev_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153020
      processors:
        - label: cpq_cmcalarm_lock2dev
          mapping: |-
            #!blobl
            root = this

            root.out.event.category.name = "Door Lock Status"
            root.out.object.name = "Door Lock 1"

            root.out.compaq.cpqCmcStatusLock2Dev = this.trap.VarBinds.index(0).Value.enum_enrich(".1.3.6.1.4.1.232.153.2.2.4.20")
        - label: cpq_cmcalarm_lock2dev_nested_switch
          switch:
            - check: ' == 1'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_1
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Status Unknown  ( Door Lock 1 )"
            - check: ' == 2'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_2
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Locked  ( Door Lock 1 )"
            - check: ' == 3'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_3
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Power Failure of Locking Device  ( Door Lock 1 )"
            - check: ' == 4'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_4
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Unlocked, Low Battery of Locking Device  ( Door Lock 1 )"
            - check: ' == 5'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_5
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Locking Device Battery Needs to be Replaced  ( Door Lock 1 )"
            - check: ' == 6'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_6
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Locking Device Battery Missing  ( Door Lock 1 )"
            - check: ' == 7'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_7
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Connection Lost to Locking Device  ( Door Lock 1 )"
            - check: ' == 8'
              processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_8
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Not Installed  ( Door Lock 1 )"
            - processors:
                - label: cpq_cmcalarm_lock2dev_nested_switch_default
                  mapping: |
                    #!blobl
                    root = this

                    root.out.event.message = "Door Lock Status Unknown  ( Door Lock 1 )"
        - label: cpq_cmcalarm_lock2dev_after_switch
          mapping: |+
            #!blobl
            root = this

    - check: this.trap.SpecificTrap == 153100
      processors:
        - label: cpq_cmc_setup_changed
          mapping: |
            #!blobl
            root = this

            root.out.event.category.name = "CMC Configuration"
            root.out.object.name = ""
            root.out.event.message = "CMC Configuration Changed"
    - processors:
        - label: default
          mapping: |
            #!blobl,
            root = this
            root.out.event.category.name = "Unknown Trap"
