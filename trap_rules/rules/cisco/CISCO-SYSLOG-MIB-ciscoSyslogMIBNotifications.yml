- mapping: |-
    #!blobl
    root = this
    root.out.origin.agent.name = "CISCO-SYSLOG-MIB"
- switch:
    - check: this.trap.SpecificTrap == 1
      processors:
        - label: clog_message_generated
          mapping: |-
            #!blobl
            root = this


            root.out.cisco.clogHistFacility = this.trap.VarBinds.index(0).Value.snmp_v2_tc_display_string()
            root.out.cisco.clogHistSeverity = this.trap.VarBinds.index(1).Value.enum_enrich(".1.3.6.1.4.1.9.9.41.1.2.3.1.3")
            root.out.cisco.clogHistMsgName = this.trap.VarBinds.index(2).Value.snmp_v2_tc_display_string()
            root.out.cisco.clogHistMsgText = this.trap.VarBinds.index(3).Value.snmp_v2_tc_display_string()
            root.out.cisco.clogHistTimestamp = this.trap.VarBinds.index(4).Value
        - label: clog_message_generated_nested_switch
          switch:
            - check: ' == 0'
              processors:
                - label: clog_message_generated_nested_switch_0
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 1'
              processors:
                - label: clog_message_generated_nested_switch_1
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 2'
              processors:
                - label: clog_message_generated_nested_switch_2
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 3'
              processors:
                - label: clog_message_generated_nested_switch_3
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 4'
              processors:
                - label: clog_message_generated_nested_switch_4
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 5'
              processors:
                - label: clog_message_generated_nested_switch_5
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 6'
              processors:
                - label: clog_message_generated_nested_switch_6
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == 7'
              processors:
                - label: clog_message_generated_nested_switch_7
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == CIP'
              processors:
                - label: clog_message_generated_nested_switch_cip
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == Unknown'
              processors:
                - label: clog_message_generated_nested_switch_unknown
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == d'
              processors:
                - label: clog_message_generated_nested_switch_d
                  mapping: |+
                    #!blobl
                    root = this

            - check: ' == dummy case statement'
              processors:
                - label: clog_message_generated_nested_switch_dummy case statement
                  mapping: |+
                    #!blobl
                    root = this

            - processors:
                - label: clog_message_generated_nested_switch_default
                  mapping: |+
                    #!blobl
                    root = this

        - label: clog_message_generated_after_switch
          mapping: |
            #!blobl
            root = this

            root.out.event.class.name = "SYSLOG-cisco-ios--"
            root.out.event.id = "SYSLOG-cisco-ios--"
            root.out.event.category.name = "-"
            root.out.event.message = this.trap.VarBinds.index(3).Value.snmp_v2_tc_display_string().string()
    - processors:
        - label: default
          mapping: |
            #!blobl,
            root = this
            root.out.event.category.name = "Unknown Trap"
